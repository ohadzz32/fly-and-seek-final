# 🏗️ Architecture Overview

## 🎯 SOLID Principles Implementation

הפרויקט מיישם את עקרונות SOLID באופן מלא:

### 1. **Single Responsibility Principle (SRP)**
- כל קומפוננטה/hook אחראי רק על משימה אחת
- הפרדה ברורה בין UI, לוגיקה, ו-state management

### 2. **Open/Closed Principle (OCP)**
- הקוד פתוח להרחבה (הוספת סוגי אזור חיפוש) אך סגור לשינוי
- שימוש ב-composition על פני inheritance

### 3. **Liskov Substitution Principle (LSP)**
- ממשקים מוגדרים בבירור (IFlight, SearchArea)
- כל היירושה שומרת על הקונטרקט של הבסיס

### 4. **Interface Segregation Principle (ISP)**
- ממשקים קטנים וממוקדים
- כל client מקבל רק את מה שהוא צריך

### 5. **Dependency Inversion Principle (DIP)**
- תלות ב-abstractions (interfaces) ולא ב-implementations
- Dependency Injection דרך props ו-hooks

---

## Backend Architecture (Clean Architecture + SOLID)

```
┌─────────────────────────────────────────────────────────────────┐
│                         HTTP Layer                               │
│  ┌────────────┐  ┌────────────┐  ┌──────────────┐              │
│  │  Express   │→│ Middleware │→│  Controllers  │              │
│  │   Server   │  │  - CORS    │  │ - Flight     │              │
│  └────────────┘  │  - Body    │  │ - Config     │              │
│                  │  - Error   │  └──────────────┘              │
│                  │  - Validate│                                  │
│                  └────────────┘                                  │
└─────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Business Logic Layer                        │
│  ┌────────────────┐                                             │
│  │ ServiceManager │                                             │
│  └────────┬───────┘                                             │
│           ↓                                                      │
│  ┌────────────────────────────────────────────────┐            │
│  │         BaseFlightService (Abstract)            │            │
│  │  - Template methods                             │            │
│  │  - Common lifecycle logic                       │            │
│  └────────┬───────────────────────────────────────┘            │
│           ↓                ↓                 ↓                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Offline    │  │  RealTime    │  │     Snap     │         │
│  │   Service    │  │   Service    │  │   Service    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                     Data Access Layer                            │
│  ┌──────────────────────────────────────────┐                  │
│  │   IFlightRepository (Interface)           │                  │
│  │   - Dependency Inversion Principle        │                  │
│  └────────────────┬─────────────────────────┘                  │
│                   ↓                                              │
│  ┌────────────────────────────────┐                            │
│  │    FlightRepository (Impl)      │                            │
│  │    - MongoDB Operations         │                            │
│  └────────────────┬───────────────┘                            │
│                   ↓                                              │
│  ┌────────────────────────────────┐                            │
│  │     Mongoose Models             │                            │
│  │     - Flight Schema             │                            │
│  └─────────────────────────────────┘                            │
└─────────────────────────────────────────────────────────────────┘
```
```

## Frontend Architecture (Hook-Based Clean Architecture)

```
┌─────────────────────────────────────────────────────────────────┐
│                      Component Layer                             │
│  ┌──────────────────────────────────────────────────┐          │
│  │               App (Main Container)                │          │
│  │  - Orchestration                                  │          │
│  │  - State composition                              │          │
│  └───────┬────────────────────────────┬─────────────┘          │
│          ↓                            ↓                          │
│  ┌──────────────┐  ┌────────────┐  ┌──────────────┐           │
│  │ ModeSelector │  │ColorPicker │  │LoadingSpinner│           │
│  └──────────────┘  └────────────┘  └──────────────┘           │
└─────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                     Custom Hooks Layer                           │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────┐     │
│  │ useFlightData  │  │ useSystemMode  │  │ useMapReady  │     │
│  │ - Data fetch   │  │ - Mode state   │  │ - Init state │     │
│  │ - Polling      │  │ - Mode switch  │  └──────────────┘     │
│  │ - Updates      │  └────────────────┘                         │
│  └────────┬───────┘                                             │
└───────────┼─────────────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Service Layer                               │
│  ┌─────────────────────────────────────────┐                   │
│  │      FlightAPIService (Static)           │                   │
│  │  - Type-safe API calls                   │                   │
│  │  - Error handling                        │                   │
│  │  - Request/Response transformation       │                   │
│  └─────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────┘
```

## Dependency Injection Flow

```
┌──────────────────────────────────────────┐
│         DIContainer (Singleton)          │
│  - Manages all dependencies              │
│  - Single source of truth                │
└────────────┬─────────────────────────────┘
             ↓
┌────────────────────────────────────────┐
│    FlightServiceFactory                 │
│    - Creates services with dependencies │
└────────────┬───────────────────────────┘
             ↓
┌────────────────────────────────────────┐
│    Services receive dependencies via    │
│    constructor injection                │
│                                          │
│    constructor(repository: IRepository) │
└──────────────────────────────────────────┘
```

## Error Handling Flow

```
Request → Validation Middleware → Controller → Service → Repository
   ↓              ↓                    ↓          ↓          ↓
Error?        Error?              Error?     Error?     Error?
   ↓              ↓                    ↓          ↓          ↓
   └──────────────┴────────────────────┴──────────┴──────────┘
                            ↓
              ┌─────────────────────────┐
              │  Global Error Handler   │
              │  - Logs error           │
              │  - Formats response     │
              │  - Returns JSON         │
              └─────────────────────────┘
```

## Data Flow Example: Update Flight Color

```
1. User clicks color button
   ↓
2. ColorPicker.onColorSelect(color)
   ↓
3. useFlightData.updateFlightColor(id, color)
   ↓
4. FlightAPIService.updateFlightColor(id, color)
   ↓
5. HTTP PATCH /api/flights/:id
   ↓
6. Validation Middleware validates color
   ↓
7. FlightController.updateFlightColor()
   ↓
8. FlightRepository.updateOne(id, { color })
   ↓
9. Mongoose Flight.findOneAndUpdate()
   ↓
10. Response flows back up the chain
   ↓
11. UI updates optimistically
```

## Key Design Patterns Used

| Pattern | Location | Purpose |
|---------|----------|---------|
| **Repository** | FlightRepository | Data access abstraction |
| **Factory** | FlightServiceFactory | Service creation |
| **Dependency Injection** | DIContainer | Manage dependencies |
| **Template Method** | BaseFlightService | Common algorithm structure |
| **Singleton** | DIContainer, Logger | Single instance |
| **Strategy** | Service implementations | Interchangeable algorithms |
| **Custom Hooks** | Frontend hooks | Reusable stateful logic |
| **Service Layer** | FlightAPIService | API abstraction |

## SOLID Principles Applied

✅ **S**ingle Responsibility: Each class has one reason to change  
✅ **O**pen/Closed: Open for extension, closed for modification  
✅ **L**iskov Substitution: Services are interchangeable  
✅ **I**nterface Segregation: Small, focused interfaces  
✅ **D**ependency Inversion: Depend on abstractions, not concretions
